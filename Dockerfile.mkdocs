# Dockerfile for Material for MkDocs documentation 

# Base Docker image with Python 3.13 on Debian Trixie slim
FROM python:3.13-trixie

# Set Python environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /mkdocs

# Install MkDocs Material (version >= 9.5)
RUN pip install --no-cache-dir "mkdocs-material>=9.5"

# Command runs in docker compose hence no CMD here
